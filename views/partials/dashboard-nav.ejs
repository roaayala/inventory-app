<nav class="dashboard-nav">
  <% dashboardMenu.forEach(menu => { %>

  <a
    href="<%= menu.link %>"
    class="nav-link <%= activeMenu.label === menu.label ? 'nav-link--active' : '' %>"
  >
    <div class="icon-<%= menu.icon %> nav-link__icon"></div>
    <span class="nav-link__label"><%= menu.label %></span>
  </a>

  <% }) %>
</nav>
